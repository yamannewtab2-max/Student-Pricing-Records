{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --parallel --filter @workspace/student-pricing --filter @workspace/api-server run dev",
    "build": "pnpm --filter @workspace/student-pricing --filter @workspace/api-server run build",
    "start": "node artifacts/api-server/dist/index.mjs",
    "typecheck": "pnpm -r run typecheck",
    "lint": "pnpm -r run typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.3",
    "@replit/vite-plugin-cartographer": "^0.6.1",
    "@replit/vite-plugin-runtime-error-modal": "^0.0.6"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  },
  "onlyBuiltDependencies": [
    "esbuild"
  ]
}
